// BookCard is now a thin re-export kept for backwards compatibility.
// The actual rendering is handled inside BookGrid (BookCover + BookInfo).
// This file is intentionally minimal.
export { } from "./BookGrid";
